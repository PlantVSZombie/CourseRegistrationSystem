<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE  mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.edu.jlu.ccst.glzz.system.generated.DAO.AdminDao">
    <resultMap id="baseResultMap" type="cn.edu.jlu.ccst.glzz.system.generated.Model.Admin">
            <result column="admin_id" property="adminId" jdbcType="VARCHAR" />
            <result column="password" property="password" jdbcType="VARCHAR" />
            <result column="name" property="name" jdbcType="VARCHAR" />
            </resultMap>
    
    <!-- 表字段 -->
    <sql id="baseColumns">
    	                	 t.`admin_id`
			                	, t.`password`
			                	, t.`name`
			            </sql>
    
    <!-- 表字段 -->
    <sql id="baseColumnsList">
    	                	        	        		<choose>
        			<when test="query.distinct">DISTINCT(t.`admin_id`)</when>
        			<otherwise>t.`admin_id`</otherwise>
        		</choose>
        	                	,        	        	t.`password`
        	                	,        	        	t.`name`
        	            </sql>
    
	<!-- 根据条件查询记录 -->
    <select id="list" parameterType="com.gitee.fastmybatis.core.query.Query"
		resultMap="baseResultMap">
		SELECT 
			<include refid="baseColumnsList" />
		FROM `admin` t
		<include refid="common.join" />
		<where>
		    <include refid="common.condition" />
		    		</where>
		<include refid="common.orderBy" />
		<include refid="common.limit" />
	</select>
	
	<!-- 查询map -->
	<select id="listMap" resultType="map">
        SELECT 
            <foreach collection="columns" item="column" separator=",">
                ${column}
            </foreach>
        FROM `admin` t
        <include refid="common.join" />
        <where>
            <include refid="common.condition" />
                    </where>
        <include refid="common.orderBy" />
        <include refid="common.limit" />
    </select>
	
	<!-- 根据条件查询记录数 -->
    <select id="getCount" parameterType="com.gitee.fastmybatis.core.query.Query"
		resultType="long">
		SELECT count(*) FROM `admin` t
		<include refid="common.join" />
        <where>
            <include refid="common.condition" />
                    </where>
    </select>

	<!-- 根据主键获取单条记录 -->
    <select id="getById" resultMap="baseResultMap" parameterType="String">
		SELECT 
        	<include refid="baseColumns" />
		FROM `admin` t
		WHERE t.`admin_id` = #{value}
				LIMIT 1
	</select>
    
    <!-- 根据条件获取单条记录 -->
    <select id="getByQuery" resultMap="baseResultMap" parameterType="com.gitee.fastmybatis.core.query.Query">
		SELECT
			<include refid="baseColumns" />			
		FROM `admin` t
		<include refid="common.join" />
        <where>
            <include refid="common.condition" />
                    </where>
		<include refid="common.orderBy" />
		LIMIT 1
	</select>
	
	<!-- 根据属性获取单条记录 -->
	<select id="getByColumn" resultMap="baseResultMap">
		SELECT
			<include refid="baseColumns" />
		FROM `admin` t
		WHERE t.`${column}` = #{value}
				LIMIT 1
	</select>
	
	<!-- 根据属性获取多条记录 -->
	<select id="listByColumn" resultMap="baseResultMap">
		SELECT
			<include refid="baseColumns" />
		FROM `admin` t
		WHERE t.`${column}`= #{value}
			</select>
	

	<!-- 保存,保存全部字段 -->
    <insert id="save" parameterType="cn.edu.jlu.ccst.glzz.system.generated.Model.Admin"
    >
	INSERT INTO `admin`
         (
                              `admin_id`
                                        , `password`
                                        , `name`
                              )
	VALUES (
                              #{adminId}
                                        , #{password}
                                        , #{name}
                     
        )
	</insert>
    
    <!-- 保存,保存不为NULL的字段 -->
    <insert id="saveIgnoreNull" parameterType="cn.edu.jlu.ccst.glzz.system.generated.Model.Admin"
            >
                INSERT INTO `admin`
    
        <trim prefix="(" suffix=")" suffixOverrides=",">	 

                                                                        <if test="adminId != null">
                       `admin_id`,
                        </if>
                                                                                                            <if test="password != null">
                       `password`,
                        </if>
                                                                                                            <if test="name != null">
                       `name`,
                        </if>
                                                        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">            
                                                                        <if test="adminId != null" >
                        #{adminId},                    
                        </if>
                                                                                                            <if test="password != null" >
                        #{password},                    
                        </if>
                                                                                                            <if test="name != null" >
                        #{name},                    
                        </if>
                                                        </trim>
    </insert>

	<!-- 批量保存,保存全部字段 -->
    <insert id="saveBatch">
		INSERT INTO `admin` 
		(
                              `admin_id`
                                        , `password`
                                        , `name`
                              )
		VALUES 
		<foreach collection="entitys" item="entity"  separator=",">
        (                              	 #{adminId}
         	                                         	, #{password}
         	                                         	, #{name}
         	                            )
		</foreach>
	</insert>


	<!-- 批量保存(兼容),保存全部字段 -->
    <insert id="saveMulti">
		INSERT INTO `admin` 
		(
                              `admin_id`
                                        , `password`
                                        , `name`
                              )
		<foreach collection="entitys" item="entity"  separator="UNION ALL">
        SELECT                               	 #{adminId}
         	                                         	, #{password}
         	                                         	, #{name}
         	                    		</foreach>
	</insert>

	<!-- 批量保存(兼容),保存全部字段,忽略重复行 -->
    <insert id="saveMultiSet">
		INSERT INTO `admin`
		(
                              `admin_id`
                                        , `password`
                                        , `name`
                              )
		<foreach collection="entitys" item="entity"  separator="UNION">
        SELECT                               	 #{adminId}
         	                                         	, #{password}
         	                                         	, #{name}
         	                    		</foreach>
	</insert>

	<!-- 更新,更新全部字段 -->
    <update id="update" parameterType="cn.edu.jlu.ccst.glzz.system.generated.Model.Admin">
    UPDATE `admin`
     <set>		
                                     `password`=#{password},
                                `name`=#{name},
                   </set>	
    WHERE `admin_id` = #{adminId}
        </update>
    
    <!-- 更新不为NULL的字段 -->
    <update id="updateIgnoreNull" parameterType="cn.edu.jlu.ccst.glzz.system.generated.Model.Admin">
    UPDATE `admin`
    <set>
    	                                                                    <if test="password != null" >
                    `password`=#{password},
                    </if>
                                                                                    <if test="name != null" >
                    `name`=#{name},
                    </if>
                                        </set>
    WHERE `admin_id` = #{adminId}
        </update>
    
    <!-- 根据指定条件更新 -->
    <update id="updateByQuery">
        UPDATE `admin`
        <set>
                                                                                                    <if test="entity.password != null or query.forceUpdate" >
                        `password`=${column.mybatisUpdateValuePrefix},
                        </if>
                                                                                                            <if test="entity.name != null or query.forceUpdate" >
                        `name`=${column.mybatisUpdateValuePrefix},
                        </if>
                                                        </set>
        <include refid="common.updateWhere" />
            </update>
    
    <!-- 根据指定条件更新 -->
    <update id="updateByMap">
        UPDATE `admin`
        <set>
            <foreach collection="entity.keys" item="key" separator=",">  
                ${key} = #{entity[${key}]}
            </foreach> 
        </set>
        <include refid="common.updateWhere" />
    </update>
    
	<!-- 根据主键删除 -->
    <delete id="deleteById" parameterType="String">
                    DELETE FROM admin
            WHERE admin_id = #{value}
            </delete>
    
    <!-- 根据条件删除记录 -->
    <delete id="deleteByQuery" parameterType="com.gitee.fastmybatis.core.query.Query">
		DELETE FROM `admin`
		<include refid="common.where" />
	</delete>

	<!--_ext_mapper_-->
	
	<!--_global_vm_-->

</mapper>