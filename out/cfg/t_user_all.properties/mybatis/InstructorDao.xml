<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE  mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.edu.jlu.ccst.glzz.system.DAO.InstructorDao">
    <resultMap id="baseResultMap" type="cn.edu.jlu.ccst.glzz.system.Model.Instructor">
            <result column="ID" property="id" jdbcType="VARCHAR" />
            <result column="name" property="name" jdbcType="VARCHAR" />
            <result column="dept_name" property="deptName" jdbcType="VARCHAR" />
            <result column="salary" property="salary" jdbcType="DECIMAL" />
            </resultMap>
    
    <!-- 表字段 -->
    <sql id="baseColumns">
    	                	 t.`ID`
			                	, t.`name`
			                	, t.`dept_name`
			                	, t.`salary`
			            </sql>
    
    <!-- 表字段 -->
    <sql id="baseColumnsList">
    	                	        	        		<choose>
        			<when test="query.distinct">DISTINCT(t.`ID`)</when>
        			<otherwise>t.`ID`</otherwise>
        		</choose>
        	                	,        	        	t.`name`
        	                	,        	        	t.`dept_name`
        	                	,        	        	t.`salary`
        	            </sql>
    
	<!-- 根据条件查询记录 -->
    <select id="list" parameterType="com.gitee.fastmybatis.core.query.Query"
		resultMap="baseResultMap">
		SELECT 
			<include refid="baseColumnsList" />
		FROM `instructor` t
		<include refid="common.join" />
		<where>
		    <include refid="common.condition" />
		    		</where>
		<include refid="common.orderBy" />
		<include refid="common.limit" />
	</select>
	
	<!-- 查询map -->
	<select id="listMap" resultType="map">
        SELECT 
            <foreach collection="columns" item="column" separator=",">
                ${column}
            </foreach>
        FROM `instructor` t
        <include refid="common.join" />
        <where>
            <include refid="common.condition" />
                    </where>
        <include refid="common.orderBy" />
        <include refid="common.limit" />
    </select>
	
	<!-- 根据条件查询记录数 -->
    <select id="getCount" parameterType="com.gitee.fastmybatis.core.query.Query"
		resultType="long">
		SELECT count(*) FROM `instructor` t
		<include refid="common.join" />
        <where>
            <include refid="common.condition" />
                    </where>
    </select>

	<!-- 根据主键获取单条记录 -->
    <select id="getById" resultMap="baseResultMap" parameterType="String">
		SELECT 
        	<include refid="baseColumns" />
		FROM `instructor` t
		WHERE t.`ID` = #{value}
				LIMIT 1
	</select>
    
    <!-- 根据条件获取单条记录 -->
    <select id="getByQuery" resultMap="baseResultMap" parameterType="com.gitee.fastmybatis.core.query.Query">
		SELECT
			<include refid="baseColumns" />			
		FROM `instructor` t
		<include refid="common.join" />
        <where>
            <include refid="common.condition" />
                    </where>
		<include refid="common.orderBy" />
		LIMIT 1
	</select>
	
	<!-- 根据属性获取单条记录 -->
	<select id="getByColumn" resultMap="baseResultMap">
		SELECT
			<include refid="baseColumns" />
		FROM `instructor` t
		WHERE t.`${column}` = #{value}
				LIMIT 1
	</select>
	
	<!-- 根据属性获取多条记录 -->
	<select id="listByColumn" resultMap="baseResultMap">
		SELECT
			<include refid="baseColumns" />
		FROM `instructor` t
		WHERE t.`${column}`= #{value}
			</select>
	

	<!-- 保存,保存全部字段 -->
    <insert id="save" parameterType="cn.edu.jlu.ccst.glzz.system.Model.Instructor"
    >
	INSERT INTO `instructor`
         (
                              `ID`
                                        , `name`
                                        , `dept_name`
                                        , `salary`
                              )
	VALUES (
                              #{id}
                                        , #{name}
                                        , #{deptName}
                                        , #{salary}
                     
        )
	</insert>
    
    <!-- 保存,保存不为NULL的字段 -->
    <insert id="saveIgnoreNull" parameterType="cn.edu.jlu.ccst.glzz.system.Model.Instructor"
            >
                INSERT INTO `instructor`
    
        <trim prefix="(" suffix=")" suffixOverrides=",">	 

                                                                        <if test="id != null">
                       `ID`,
                        </if>
                                                                                                            <if test="name != null">
                       `name`,
                        </if>
                                                                                                            <if test="deptName != null">
                       `dept_name`,
                        </if>
                                                                                                            <if test="salary != null">
                       `salary`,
                        </if>
                                                        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">            
                                                                        <if test="id != null" >
                        #{id},                    
                        </if>
                                                                                                            <if test="name != null" >
                        #{name},                    
                        </if>
                                                                                                            <if test="deptName != null" >
                        #{deptName},                    
                        </if>
                                                                                                            <if test="salary != null" >
                        #{salary},                    
                        </if>
                                                        </trim>
    </insert>

	<!-- 批量保存,保存全部字段 -->
    <insert id="saveBatch">
		INSERT INTO `instructor` 
		(
                              `ID`
                                        , `name`
                                        , `dept_name`
                                        , `salary`
                              )
		VALUES 
		<foreach collection="entitys" item="entity"  separator=",">
        (                              	 #{id}
         	                                         	, #{name}
         	                                         	, #{deptName}
         	                                         	, #{salary}
         	                            )
		</foreach>
	</insert>


	<!-- 批量保存(兼容),保存全部字段 -->
    <insert id="saveMulti">
		INSERT INTO `instructor` 
		(
                              `ID`
                                        , `name`
                                        , `dept_name`
                                        , `salary`
                              )
		<foreach collection="entitys" item="entity"  separator="UNION ALL">
        SELECT                               	 #{id}
         	                                         	, #{name}
         	                                         	, #{deptName}
         	                                         	, #{salary}
         	                    		</foreach>
	</insert>

	<!-- 批量保存(兼容),保存全部字段,忽略重复行 -->
    <insert id="saveMultiSet">
		INSERT INTO `instructor`
		(
                              `ID`
                                        , `name`
                                        , `dept_name`
                                        , `salary`
                              )
		<foreach collection="entitys" item="entity"  separator="UNION">
        SELECT                               	 #{id}
         	                                         	, #{name}
         	                                         	, #{deptName}
         	                                         	, #{salary}
         	                    		</foreach>
	</insert>

	<!-- 更新,更新全部字段 -->
    <update id="update" parameterType="cn.edu.jlu.ccst.glzz.system.Model.Instructor">
    UPDATE `instructor`
     <set>		
                                     `name`=#{name},
                                `dept_name`=#{deptName},
                                `salary`=#{salary},
                   </set>	
    WHERE `ID` = #{id}
        </update>
    
    <!-- 更新不为NULL的字段 -->
    <update id="updateIgnoreNull" parameterType="cn.edu.jlu.ccst.glzz.system.Model.Instructor">
    UPDATE `instructor`
    <set>
    	                                                                    <if test="name != null" >
                    `name`=#{name},
                    </if>
                                                                                    <if test="deptName != null" >
                    `dept_name`=#{deptName},
                    </if>
                                                                                    <if test="salary != null" >
                    `salary`=#{salary},
                    </if>
                                        </set>
    WHERE `ID` = #{id}
        </update>
    
    <!-- 根据指定条件更新 -->
    <update id="updateByQuery">
        UPDATE `instructor`
        <set>
                                                                                                    <if test="entity.name != null or query.forceUpdate" >
                        `name`=${column.mybatisUpdateValuePrefix},
                        </if>
                                                                                                            <if test="entity.deptName != null or query.forceUpdate" >
                        `dept_name`=${column.mybatisUpdateValuePrefix},
                        </if>
                                                                                                            <if test="entity.salary != null or query.forceUpdate" >
                        `salary`=${column.mybatisUpdateValuePrefix},
                        </if>
                                                        </set>
        <include refid="common.updateWhere" />
            </update>
    
    <!-- 根据指定条件更新 -->
    <update id="updateByMap">
        UPDATE `instructor`
        <set>
            <foreach collection="entity.keys" item="key" separator=",">  
                ${key} = #{entity[${key}]}
            </foreach> 
        </set>
        <include refid="common.updateWhere" />
    </update>
    
	<!-- 根据主键删除 -->
    <delete id="deleteById" parameterType="String">
                    DELETE FROM instructor
            WHERE ID = #{value}
            </delete>
    
    <!-- 根据条件删除记录 -->
    <delete id="deleteByQuery" parameterType="com.gitee.fastmybatis.core.query.Query">
		DELETE FROM `instructor`
		<include refid="common.where" />
	</delete>

	<!--_ext_mapper_-->
	
	<!--_global_vm_-->

</mapper>